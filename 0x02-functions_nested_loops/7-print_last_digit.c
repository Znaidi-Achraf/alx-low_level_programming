#include "main.h"

/**
 * print_last_digit - print the last digit
 *
 * @n: number used to find the last digit
 *
 * Return: last digit
 */

int print_last_digit(int n)
{
	int 1;

	1 = n % 10
		if (1 < 0)
		{
			_putchar(-1 + 48);
			return (-1);
		}
		else
		{
			_putchar(1 + 48);
			return (1);
		}
}
